# Copy to .env and fill with your Jira details
# PORT=3000

# Jira Cloud example:
# JIRA_BASE_URL=https://your-domain.atlassian.net
# JIRA_EMAIL=you@example.com
# JIRA_API_TOKEN=atlassian_api_token

# Jira Server/DC example (use PAT):
# JIRA_BASE_URL=https://jira.your-company.com
# JIRA_PAT=your_pat_here
# JIRA_AUTH_TYPE=bearer
# JIRA_API_VERSION=2

# Optional overrides (auto-detected otherwise)
# Optional, to attribute author explicitly (else Jira uses the auth user)
# JIRA_ACCOUNT_ID=xxxxxxxxxxxxxxxxxxxxxxxx

# Optional: directory containing additional corporate CA certs (.crt/.cer/.pem)
# Put your CA files in this folder and set the absolute path here.
# EXTRA_CA_DIR=
# Alternatively, you can provide a single bundle file via standard Node var
# NODE_EXTRA_CA_CERTS=C:\\Certs\\corp-root.cer
